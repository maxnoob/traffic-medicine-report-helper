const app = Vue.createApp({
    template:'<h2>vue test</h2>'
})

app.mount('#app')


/*
const { createApp, template } = Vue

createApp({
    setup() {
        return {
            template: '<h2>vue part</h2>'
          }
    }
}).mount('#app')
*/